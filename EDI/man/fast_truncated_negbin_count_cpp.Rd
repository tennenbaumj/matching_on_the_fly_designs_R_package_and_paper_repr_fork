% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fast_truncated_negbin_count_cpp}
\alias{fast_truncated_negbin_count_cpp}
\title{Fit a zero-truncated negative binomial count model}
\usage{
fast_truncated_negbin_count_cpp(
  X_r,
  y_r,
  warm_start_params = NULL,
  smart_cold_start = TRUE,
  estimate_only = FALSE,
  maxit = 1000L,
  tol = 1e-08,
  fixed_idx = NULL,
  fixed_values = NULL,
  optimization_alg = "lbfgs",
  warm_start_fisher_info = NULL
)
}
\arguments{
\item{X_r}{Design matrix (n x p, intercept column included).}

\item{y_r}{Integer response vector of positive counts (zero-truncated; all values must be >= 1).}

\item{warm_start_params}{Optional numeric vector of length \code{ncol(X_r) + 1} giving starting
values for \code{(beta, log_theta)}. Validated on entry; an out-of-range \code{log_theta}
(outside \[-20, 20\]) triggers an error.}

\item{smart_cold_start}{If \code{TRUE} (default) and \code{warm_start_params} is \code{NULL},
uses data-driven heuristics to choose a starting point.}

\item{estimate_only}{If \code{TRUE}, skips Fisher information computation and returns only
the point estimate. Default \code{FALSE}.}

\item{maxit}{Maximum number of optimizer iterations. Default 1000.}

\item{tol}{Convergence tolerance. Default 1e-8.}

\item{fixed_idx}{Optional integer vector of 1-based parameter indices to hold fixed.}

\item{fixed_values}{Optional numeric vector of values for the fixed parameters (same length as
\code{fixed_idx}).}

\item{optimization_alg}{Optimizer to use. One of \code{"lbfgs"} (default) or \code{"bfgs"}.}

\item{warm_start_fisher_info}{Optional square numeric matrix of size \code{ncol(X_r) + 1}
giving a starting Fisher information matrix.}
}
\value{
A named list with elements \code{converged} (logical), \code{params} (numeric vector of
  length \code{ncol(X_r) + 1}), \code{b} (the regression coefficients without \code{log_theta}),
  \code{neg_ll} (the negative log-likelihood), and \code{fisher_information} (the estimated
  Fisher information matrix, or \code{NULL} when \code{estimate_only = TRUE}).
}
\description{
Fit a zero-truncated negative binomial count model
}
